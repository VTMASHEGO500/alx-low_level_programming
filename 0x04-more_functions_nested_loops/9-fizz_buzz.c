#include "main.h"

/**
 *main - print the number from 1 to 100
 *
 *
 * Return: 0
 */
int main(void)
{
	int n;

	for (n =1; n <= 100; n++)
	{
		if (n % 3 == 0 && n % 5 ! =0)
		{
			prinft(" fuzz")
		} else if (n % 5 == 0 && i % 3 ! = 0)
		{
			printf(" buzz")
		} else if (n % 3 == 0 && ! % 5 == 0)
		{
			printf(" fuzzbuzz");
		} else if (n == 1)
		{
			printf("%d", n);
		} else
		{
			printf(" %d", n);
		}
	}
	printf("\n")

	return: (0);

